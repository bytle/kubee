% kubee-helm(1) Version Latest | Helm with KubeConfig
# NAME

`kubee-helm` is a `helm` wrapper that gets the `kubeconfig` information from the `pass` secret manager.

See [kubee-config](../kubeconfig-pass.md)
